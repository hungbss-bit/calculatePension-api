# Khóa bí mật cho GPT Action. Không thêm dấu nháy hoặc chữ Bearer.
API_KEY=replace-with-a-long-random-secret

# Trên Render nên đặt true để không vô tình chạy API không có khóa.
REQUIRE_API_KEY=true

# Chỉ bật tạm thời để chẩn đoán X-API-Key, sau đó đặt lại false.
AUTH_DIAGNOSTICS_ENABLED=false
